# Architecture Review Standards

## 必须满足
- 模块边界清晰，职责单一
- 模块间接口契约明确（API、数据模型、事件格式）
- 技术选型有合理的理由和对比
- 实现任务已拆解且耦合关系标注正确
- 识别了关键技术风险

## 不允许
- 过度设计（YAGNI：不需要的抽象不留）
- 缺少接口定义的模糊描述
- 缺少实现任务拆解（Engine 依赖此拆解派发任务）

_安装后按项目架构规范调整。_
